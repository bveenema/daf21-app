import React from 'react';
import { shallow } from 'enzyme';
import MixStationExpansionPanel from './MixStationExpansionPanel';

it('renders without crashing', () => {
  shallow(<MixStationExpansionPanel />);
});
